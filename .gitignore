**/.DS_Store

# Terraform local/runtime files
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Generated private config
.local/
backend.hcl
terraform.auto.tfvars.json

# Local environment files
.env
.env.*
targets/aks/terraform/local-operation.env

# Operator evidence is local by default. Only the historical summary and
# reviewed, deliberately sanitized dated summaries may be committed.
targets/aks/evidence/*
!targets/aks/evidence/validation-summary.json
!targets/aks/evidence/validation-[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].json
